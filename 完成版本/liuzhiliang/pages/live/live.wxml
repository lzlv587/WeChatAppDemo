<view class='frame'>
  <block wx:for='{{liveList}}' wx:key=''>
  <!-- js中的movieList在这里使用 -->
    <view class='item'>
      <image src='{{item.poster}}' bindtap='toPlay' data-vid='{{item.vid}}'></image>
      <text class="text">{{item.title}}</text>
    </view>
  </block>

</view>